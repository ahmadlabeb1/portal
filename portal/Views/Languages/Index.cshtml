@model IEnumerable<portal.Models.Language>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Lang_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lang_key)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Lang_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lang_key)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Lang_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Lang_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Lang_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
